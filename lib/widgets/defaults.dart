// Database that other pages will use to unify color scheme, just defaults unless we get settings.dart to edit this globally

// Created by Mark Joshua Zuniga

import 'package:flutter/material.dart';

var screencolorL = Colors.white;
var textcolorL = Colors.black;
var tabcolorL = Colors.amber[400];

var screencolorB = Colors.black87;
var textcolorB = Colors.white;
var tabcolorB = Colors.amber[400];

var defaultAddAssetValue = 0;
var defaultAddAssetName = 'Asset';

// would work if somehow we can just ping the things from settings.dart to other pages and back, really.
